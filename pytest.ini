[pytest]
pythonpath = .
markers =
    unit: unit tests — no external dependencies
    integration: end-to-end scenario tests using mocked HTTP and in-memory DB

addopts = -v --tb=short
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
